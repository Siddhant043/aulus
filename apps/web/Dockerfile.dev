FROM oven/bun:1.3
WORKDIR /app
COPY package.json bun.lock turbo.json ./
COPY apps ./apps
COPY packages ./packages
RUN bun install --frozen-lockfile
EXPOSE 5173
CMD ["bun", "run", "--filter", "@aulus/web", "dev", "--", "--host", "0.0.0.0"]
